apiVersion: v1
stringData:
  database: sonarqubedb
  host: <config.{{.toolchains.postgresql.host | default "host"}}>
  jdbc-password: <config.{{.toolchains.postgresql.password | default "password"}}>
  port: '<config.{{.toolchains.postgresql.port | default "5432"}}>'
  username: <config.{{.toolchains.postgresql.username | default "postgres"}}>
kind: Secret
metadata:
  name: sonarqube-pg
type: Opaque