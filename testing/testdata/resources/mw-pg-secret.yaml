apiVersion: v1
stringData:
  database: sonarqubedb
  host: <config.{{.toolchains.postgresql-14.host | default "host"}}>
  jdbc-password: <config.{{.toolchains.postgresql-14.password | default "password"}}>
  port: '<config.{{.toolchains.postgresql-14.port | default "5432"}}>'
  username: <config.{{.toolchains.postgresql-14.username | default "postgres"}}>
kind: Secret
metadata:
  name: sonarqube-pg
type: Opaque